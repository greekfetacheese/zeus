[package]
name = "currency"
version = "0.1.0"
edition = "2024"

[dependencies]
abi = { path = "../abi" }
utils = { path = "../utils" }
types = { path = "../types" }


tokio = {version = "1", features = ["rt-multi-thread"]}
serde = "1"
anyhow = "1"

[dev-dependencies]
url = "2"

## 2026-06-29 - Shield/Unshield Builders Started

- Added `crates/zeus-railgun/src/builders.rs`
- Implemented:
  - `PreparedShield` struct + `prepare_shield()` and `build_shield_call_data()`
  - `PreparedUnshield` struct + `prepare_unshield()` (uses scanner unspent notes + merkle proofs)
  - `apply_shield_to_scanner()` and `apply_unshield_to_scanner()`
- Re-exported from lib.rs
- 2 dedicated tests + full suite now **19 passing**
- Uses existing Note creation, contract sol! structs, scanner state, and PoseidonMerkleTree::get_proof
- Current simplifications (to be refined):
  - Shield uses basic Note::new (not full blinded create_note_with_keys)
  - Shield ciphertext uses fixed shared key (derivation alignment pending with keys module)
- Roadmap and todos updated

Next: broadcaster fee integration, proper change notes, real shieldKey derivation, wire into Zeus.


Refinements 2026-06-29 (ponytail):
- Shield: full blinded via get_note_blinding_keys(shield_key) + derive_shared.
- Unshield: multi select + change_note, now needs keys.
- Reused existing funcs. No new deps. Tests 19 green.

## 2026-06-30 Review + Waku Wiring Start

**Review performed**:
- Inspected all current railgun + waku-broadcaster code.
- cargo check + full test suite green.
- Architecture still excellent (no cycle risk).

**Verdict**: Ready to start wiring.

**What we added for optional gas-sponsored unshield**:
- PreparedBroadcasterUnshield struct
- prepare_unshield_for_broadcaster + method on RailgunEngine
- Takes fees_id + broadcaster address + min gas price (from waku client.get_best_fee_quote)
- Re-exported
- Skeleton only (full transact calldata assembly pending — next logical step)


## 2026-06-30 - Real Transact Calldata + use_broadcaster Flag

- Added `transact(Transaction[])` and all supporting structs (Transaction, BoundParams, SnarkProof, G1/G2Point, UnshieldType) to contracts.rs sol! block.
- Implemented `build_unshield_transact_calldata(scanner, prepared_unshield, chain_id, min_gas_price, use_broadcaster)` that produces real encoded calldata for the contract.
- Updated high-level unshield API:
  - `RailgunEngine::prepare_unshield(..., use_broadcaster: bool)`
  - low-level `prepare_unshield(..., _use_broadcaster: bool)`
- `prepare_unshield_for_broadcaster` now calls the builder and populates `transact_calldata`.
- Added convenience method on RailgunEngine.
- All calls now respect the flag for unshield (shields don't use it).
- cargo check clean, 19 tests pass.

This completes the "build the real transact calldata" step and makes the broadcaster path usable from the engine.


## 2026-06-30 - Fleshed out remaining details (pre-Waku wiring)

**Completed:**
- ZK proof: introduced `create_dummy_snark_proof()` with strong documentation that real Groth16 from Railgun circuit is still needed.
- Chain ID: no more hard-coded values. Engine + scanner provide it. Convenience methods added.
- Note selection: largest-first greedy (better for broadcaster unshield).
- Change note: confirmed working + documented in calldata + unshield_preimage.
- Updated docs, removed stale TODO comments, added 2 new tests (now 21 passing).
- All previous functionality preserved.

Engine is now in much better shape for the next phase (wiring optional broadcaster fees from zeus-waku-broadcaster).


## 2026-06-30 - Prover Sidecar Scaffolding Started

- Decision: Build `zeus-railgun-prover` crate using the exact same sidecar pattern as `zeus-waku-broadcaster`.
- Created crate skeleton:
  - `Cargo.toml`
  - `src/lib.rs`
  - `src/client.rs` (RailgunProverClient — spawns node, line JSON protocol)
  - `js-sidecar/package.json`
  - `js-sidecar/src/index.js` (artifact download from Railgun IPFS + snarkjs.groth16.fullProve)
- Added to workspace.
- `cargo check -p zeus-railgun-prover` — clean.
- Memory + RAILGUN_ROADMAP.md updated.
- Next: Wire real witness format from builders + integrate with RailgunEngine.

